---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';

const posts = (await getCollection('posts'))
  .filter((p) => p.data.status === 'ready')
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime());

const featured = posts[0];
const rest = posts.slice(1);
---
<BaseLayout title="yaqlab.com" description="Praktyczny blog o AI, infrastrukturze i procesach">
  <section style="margin-bottom:1.5rem;">
    <h1>yaqlab.com</h1>
    <p class="meta">
      Praktyczne notatki o AI, infrastrukturze i procesach — tworzone z rozmów,
      redagowane i publikowane przez workflow GitHub PR.
    </p>
  </section>

  {featured ? (
    <section>
      <h2>Wyróżniony wpis</h2>
      <article class="card" style="margin-bottom:1.5rem;">
        <a href={`posts/${featured.slug}/`} style="display:block;border-radius:14px;overflow:hidden;margin-bottom:.8rem;">
          <img
            src={featured.data.hero_image}
            alt={featured.data.hero_alt}
            loading="lazy"
            style="display:block;width:100%;height:clamp(170px,36vw,340px);object-fit:cover;"
          />
        </a>
        <p class="meta">{featured.data.date.toISOString().slice(0,10)} · {featured.data.lang.toUpperCase()}</p>
        <h3 style="margin:.2rem 0 .7rem;">
          <a href={`posts/${featured.slug}/`}>{featured.data.title}</a>
        </h3>
        <p>{featured.data.excerpt}</p>
        <p><a href={`posts/${featured.slug}/`}>Czytaj wpis →</a></p>
      </article>
    </section>
  ) : (
    <section class="card">
      <h2>Wpisy pojawią się wkrótce</h2>
      <p>Gdy opublikujesz pierwszy wpis ze statusem <code>ready</code>, pojawi się tutaj automatycznie.</p>
    </section>
  )}

  {rest.length > 0 && (
    <section>
      <h2>Ostatnie wpisy</h2>
      <ul style="list-style:none;padding:0;display:grid;gap:.8rem;">
        {rest.map((post) => (
          <li class="card" style="display:grid;grid-template-columns:minmax(110px,160px) 1fr;gap:.8rem;align-items:center;">
            <a href={`posts/${post.slug}/`} style="display:block;border-radius:12px;overflow:hidden;">
              <img
                src={post.data.hero_image}
                alt={post.data.hero_alt}
                loading="lazy"
                style="display:block;width:100%;height:96px;object-fit:cover;"
              />
            </a>
            <div>
              <a href={`posts/${post.slug}/`}><strong>{post.data.title}</strong></a>
            <div class="meta">{post.data.date.toISOString().slice(0,10)} · {post.data.lang.toUpperCase()}</div>
            </div>
          </li>
        ))}
      </ul>
    </section>
  )}
</BaseLayout>
